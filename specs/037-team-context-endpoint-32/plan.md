# Implementation Plan: Team Context Endpoint

**Branch**: `037-team-context-endpoint-32` | **Date**: 2026-03-13 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/037-team-context-endpoint-32/spec.md`

## Summary

Add `PATCH /api/v2/users/me/team-context` to the master api-reference.md, update the `GET /users/me` entry to document that `current_team` is now reliably populated, add a `use {team_id}` action to `rkit:teams` to set the active team via the new endpoint, then sync all skill copies via `/sync-plugin`.

## Technical Context

**Language/Version**: Bash 5.x + Markdown (Claude Code skill runtime)
**Primary Dependencies**: `scripts/api.sh`, `curl`, `jq`
**Storage**: N/A — all data via ResultMaps V2 API
**Testing**: Manual API verification via `scripts/api.sh`
**Target Platform**: Claude Code skill runtime (any OS)
**Project Type**: Single project (skill suite)
**Performance Goals**: N/A — response time governed by external API
**Constraints**: All skills must comply with the ResultKit v1 Constitution
**Scale/Scope**: api-reference.md update + one new action in `rkit:teams` SKILL.md

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Claude Code Skill Format | ✅ PASS | Changes land in SKILL.md + api-reference.md |
| II. Self-Contained | ✅ PASS | New action uses same api.sh + config pattern |
| III. Config-Driven | ✅ PASS | Token read from config.json; no hardcoded values |
| IV. Confirm Writes | ✅ PASS | PATCH endpoint requires confirmation before execution |
| V. Show IDs | ✅ PASS | Response displays team id and name |
| VI. Framework-Aware | N/A | Not framework-specific |
| VII. Direct Execution | ✅ PASS | Uses Bash + api.sh directly; no Task agents |
| VIII. Graceful Degradation | ✅ PASS | 401 → re-run setup; 422 → human-readable error |
| IX. Concise Output | ✅ PASS | Single-line confirmation on success |

No violations. No complexity tracking required.

## Project Structure

### Documentation (this feature)

```text
specs/037-team-context-endpoint-32/
├── plan.md              # This file
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output
├── quickstart.md        # Phase 1 output
└── tasks.md             # Phase 2 output (/speckit:tasks)
```

### Source Code (repository root)

```text
api-reference.md                          # Master API reference — add new endpoint, update current_team note
skills/teams/SKILL.md                     # Add `use {team_id}` action for set-team-context
```

All `skills/*/references/api-reference.md` copies are regenerated by `/sync-plugin` — not edited directly.

**Structure Decision**: Single-project, no new files. One SKILL.md update + one api-reference.md update, then sync.
