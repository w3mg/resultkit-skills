# This repo is publish output

This repo (`w3mg/resultkit-skills`) is generated by `scripts/publish.sh` from `w3mg/resultkit-skills-dev`.

Never commit, edit, or file work here — direct changes are deleted on the next publish.

All development, BDDs/specs, and issues live in `resultkit-skills-dev`.
